import { User } from './User'

// Exmaple of associations
// User.belongsToMany(Episode, { through: WatchTime })
// User.hasMany(Favorite, { as: 'FavoritesCourses', foreignKey: 'user_id' })

export { User }
